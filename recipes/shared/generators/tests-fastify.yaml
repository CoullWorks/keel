# Test suite as a level: stack generator. Points at the existing node-vitest addon,
# so `keel gen tests` on a fastify project adds its standard runner. Thin pointer,
# not a second copy of how the runner installs.
id: gen-tests-fastify
kind: generator
level: stack
label: "Test suite (Vitest)"
appliesTo: [fastify]
provides: [tests]
apply: [node-vitest]
schema_version: 2
updated: 2026-08-24
