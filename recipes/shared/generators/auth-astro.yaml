# Auth as a level:stack generator (better-auth). Points at the astro-better-auth
# addon that already exists. Thin pointer, not a second copy of how the addon
# installs.
id: gen-auth-astro
kind: generator
level: stack
label: "Authentication (better-auth: email + password, sessions, protected routes)"
appliesTo: [astro]
provides: [auth]
apply: [astro-better-auth]
schema_version: 2
updated: 2026-08-24
