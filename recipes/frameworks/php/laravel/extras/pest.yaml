id: pest
kind: extra
label: Pest (tests)
appliesTo: [laravel]
provides: [tests]
default: true
install:
  - "{{composer}} require pestphp/pest --dev --with-all-dependencies"
  - "{{exec}} ./vendor/bin/pest --init"
